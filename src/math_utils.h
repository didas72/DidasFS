//math_utils.h - Math utilities

#ifndef DIDASFS_MATH_UTILS_H
#define DIDASFS_MATH_UTILS_H

#define MIN(a, b) ((a) < (b) ? (a) : (b))
#define MAX(a, b) ((a) > (b) ? (a) : (b))
#define DIV_ROUND_UP(a, b) (((a - 1) / b) + 1)

#endif
